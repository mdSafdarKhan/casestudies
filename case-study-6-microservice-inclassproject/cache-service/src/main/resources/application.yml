spring:
  datasource:
    url: jdbc:mysql://localhost:3306/learning
    username: root
    password: jiyodigital
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  #cache:
   # type: redis
  #redis:
   # host: localhost
    #port: 6379